{
  "name": "ats-generator-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package config for automated cloud deployments (Railway / Vercel)",
  "scripts": {
    "postinstall": "cd server && npm install",
    "build": "cd server && npm install",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
